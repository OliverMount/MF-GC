# No semi-colon for opening 

.open <data_base_name>
.tables 
.mode box

select count(*) from nodes where cell_type="mf";
(or)
select count(*) from nodes where cell_type like "mf";

SELECT DISTINCT cell_type FROM nodes;

select count(*) from nodes where cell_type LIKE "%mf%";
pragma table_info('nodes')

.quit
